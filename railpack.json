{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "sh -c 'cd src && python manage.py migrate --noinput && exec gunicorn home.wsgi:application --bind 0.0.0.0:${PORT:-8000}'"
  }
}
